#!/usr/bin/awk -f

# Take an undirected sif of the form:
#
#	gene <tab> mi <tab> gene
#
# Print the genes in alphabetic order so that repeats can be found
# and removed later.

BEGIN {
	FS="\t"
	OFS="\t"
}

$1 != $3 {
	if($1 < $3) {
		print $0
	} else {
		print $3 OFS $2 OFS $1
	}
}
